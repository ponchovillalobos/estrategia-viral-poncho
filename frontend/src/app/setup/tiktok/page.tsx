import { TikTokSetupClient } from "@/components/setup/tiktok-setup-client";

export const metadata = {
  title: "Conectar TikTok — Estrategia Viral Poncho",
};

export default function TikTokSetupPage() {
  return <TikTokSetupClient />;
}
